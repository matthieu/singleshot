en:
  activities: # Views
    show:
      title: "Activities"
      subtitle: "Activity from all tasks you own or observe"
    sidebar:
      heading:  "Activity"
      link:     "More &hellip;"
  activity: # Expansion
    task:
      created:    "{{person}} created {{task}}"
      claimed:    "{{person}} is owner of {{task}}"
      delegated:  "{{person}} delegated {{task}}"
      modified:   "{{person}} modified {{task}}"
      suspended:  "{{person}} suspended {{task}}"
      resumed:    "{{person}} resumed {{task}}"
      released:   "{{person}} no longer owner of {{task}}"
      cancelled:  "{{person}} cancelled {{task}}"
      completed:  "{{person}} completed {{task}}"
    template:
      created:    "{{person}} created the template {{task}}"
      enabled:    "{{person}} enabled the template {{task}}"
      disabled:   "{{person}} disabled the template {{task}}"
      modified:    "{{person}} changed the template {{task}}"
